from django.apps import AppConfig

class FormasPagamentoConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'formas_pagamento'
    verbose_name = 'Formas de Pagamento'
